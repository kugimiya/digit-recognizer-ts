{
  "name": "neural",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc && node dist/index.js",
    "build": "tsc",
    "run-front": "tsc --project tsconfig.front.json && npx serve --ssl-cert serve-https-keys/cert.pem --ssl-key serve-https-keys/key.pem ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^20.12.7",
    "etp-ts": "^1.0.5",
    "typescript": "^5.4.5",
    "worker-farm": "^1.7.0"
  }
}
